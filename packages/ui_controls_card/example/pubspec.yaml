name: app_card_example
description: Example app demonstrating the AppCard component
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  ui_controls_card:
    path: ../
  ui_tokens:
    path: ../../ui_tokens
  ui_core:
    path: ../../ui_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
